# ==================================================
# HAL Answering Service -- Configuration
# ==================================================
# Copy to .env and fill in your values:  cp .env.example .env

# --- SignalWire (required) ---
SIGNALWIRE_PROJECT_ID=your-project-id
SIGNALWIRE_TOKEN=your-api-token
SIGNALWIRE_SPACE=your-space-name
SIGNALWIRE_PHONE_NUMBER=+1XXXXXXXXXX
# Webhook signing key (optional -- falls back to SIGNALWIRE_TOKEN if unset)
# SIGNALWIRE_SIGNING_KEY=

# --- Server ---
HOST=0.0.0.0
PORT=8080
# Your public HTTPS hostname (e.g. from Tailscale Funnel, Cloudflare Tunnel, ngrok).
# Do NOT include https:// -- just the hostname.
PUBLIC_HOST=your-hostname.example.com

# --- Owner ---
# Name used in greetings: "You've reached {name}'s phone."
OWNER_NAME=

# --- STT (Faster-Whisper) ---
STT_MODEL=large-v3-turbo
STT_DEVICE=cuda
STT_COMPUTE_TYPE=float16

# --- LLM (OpenAI-compatible API, e.g. LM Studio) ---
LLM_BASE_URL=http://127.0.0.1:1234/v1
LLM_API_KEY=lm-studio
LLM_MODEL=zai-org/glm-4.7-flash
LLM_MAX_TOKENS=200
LLM_TEMPERATURE=0.7

# --- TTS (Chatterbox Turbo) ---
# Hugging Face token -- required to download the model on first run.
# Get one at: https://huggingface.co/settings/tokens
HF_TOKEN=
# Path to a WAV file (>5s) for voice cloning. Leave empty for default voice.
# Two samples are included: hal9000.wav and eugene.wav
TTS_VOICE_PROMPT=

# --- VAD (Silero) ---
VAD_SPEECH_THRESHOLD=0.5
VAD_SILENCE_THRESHOLD_MS=400
VAD_MIN_SPEECH_MS=250

# --- Security ---
MAX_CONCURRENT_CALLS=3
MAX_CALL_DURATION_S=600

# --- Recording ---
RECORDINGS_DIR=recordings

# --- Notifications (optional) ---
# ntfy.sh topic for push notifications with call summaries.
# Create a topic at https://ntfy.sh and put the topic name here.
# NTFY_TOPIC=
